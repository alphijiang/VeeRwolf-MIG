# SPDX-License-Identifier: Apache-2.0

# Optional VeeR EH2 Nexys clock override. This file is loaded explicitly with
# EXTRA_CONF_FILE and does not alter the upstream EH1/EL2 board default.
CONFIG_SYS_CLOCK_HW_CYCLES_PER_SEC=40000000
